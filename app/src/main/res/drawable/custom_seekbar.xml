<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- background -->
    <item>
        <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="line">
            <stroke android:width="7dp" android:color="@color/transparent35"/>
            <corners android:radius="7dp" />
        </shape>
    </item>

    <!-- progress -->
    <item>

        <!-- clip to only display progress -->
        <clip>
            <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="line">
                <stroke android:width="7dp" android:color="@color/colorRed"/>
                <corners android:radius="7dp" />
            </shape>
        </clip>
    </item>

</layer-list>